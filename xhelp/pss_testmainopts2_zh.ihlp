
{help pss_testmainopts2:English Version}
{hline}
{* *! version 1.0.4  20oct2016}{...}
{synopt: {opt dir:ection}({opt l:ower}|{opt u:pper})}效应方向 
   用于效应大小的确定；默认值是
   {cmd:direction(lower)}，这意味着参数的假设值小于假设值{p_end}
{synopt :{opt onesid:ed}}单尾检验；默认值为双尾{p_end}
{synopt: {opt par:allel}}将星号选项或命令参数中的数字列表视为并行，当每个选项或参数指定多个值时（不要列举所有可能的值组合）
{p_end}